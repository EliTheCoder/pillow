proc fibonacci
    int -> int
do
    dup dup -- && ! if else
        dup 1 - fibonacci
        swp 2 - fibonacci
        +
    end
end

1 while dup 20 <= then
    dup
    fibonacci println
    ++
end
